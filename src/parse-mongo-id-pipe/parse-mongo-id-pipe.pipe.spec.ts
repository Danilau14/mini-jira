import { ParseMongoIdPipe } from './parse-mongo-id-pipe.pipe';

describe('ParseMongoIdPipe', () => {
  it('should be defined', () => {
    // eslint-disable-next-line @typescript-eslint/ban-ts-comment
    // @ts-expect-error
    expect(new ParseMongoIdPipe()).toBeDefined();
  });
});
